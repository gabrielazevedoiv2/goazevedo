import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import { Paper } from '@material-ui/core';

export default function LearningsPage(props) {
    return (
        <Paper>
            MyLearnings
        </Paper>
    )
}